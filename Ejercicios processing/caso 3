int cant = 10; 
float [] circulos = new float[cant];

void setup(){
 size(800,600);
 
}
void draw(){
 float px=0;
  for(int i=0; i<cant; i++){
    circulos[i]= map(i, 0, cant, 0, 300);
    px=px + (circulos[i]/2);
    circle(px,height/2,circulos[i]);
   px=px + (circulos[i]/2);
  }
}
